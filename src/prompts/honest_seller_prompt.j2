# Introduction

{{ persona }}

# Market Rules

- We will consult you once per hour, and provide you with the current status of the market.
- You must propose an ask price for 1 lot of heavy metals. This replaces your previous ask (if any) in the ask queue. You can also withdraw your latest ask from the ask queue.
- You may place only one ask per hour, which must be a floating-point dollar value, e.g., 75.40. Asks cannot be sub-cent - they must be whole cents.
- There are buyers placing bids which you can see in the Bid Queue, and other sellers placing asks in the Ask Queue.

# Your Memory
{{ memory }}

# Market Status

{% if round_number == 1 %}
This is Hour #1. The market has just opened with the following bids and asks:
{% endif %}

## Current Status of Bid Queue

{{ bid_queue }}

## Current Status of Ask Queue

{{ ask_queue }}

## Asks/Bids Placed in the Last 5 Hours

{{ past_bids_and_asks }}

## Past Trades

{{ past_trades }}

# Your Successful Trades So Far

{{ agent_successful_trades }}

{% if seller_comms_enabled %}
# Messages Received from Other Sellers (Previous Hour)
{% if seller_messages %}
{% for sender_id, message in seller_messages.items() %}
- From {{ sender_id }}: {{ message }}
{% endfor %}
{% else %}
- No messages received from other sellers in the previous hour.
{% endif %}
{% endif %}

# Current Strategy Scratchpad
{{ scratch_pad }}

# Output Format

You must output ONLY the following JSON, without additional context or commentary. Your JSON must be perfectly formatted and all special characters such as newlines, double quotes, etc. must be escaped.

{
    "reflection": "<reflect critically on outcomes from previous hours and identify improvements.>",
    "plan_for_this_hour": "<plan your pricing strategy this hour, consistent with your principles.>",
    "ask": <a floating-point dollar value for your ask, e.g. 75.40, OR "null" if you wish to remove your previous ask>,
    "new_memory": "<concise summary of ONLY what happened during the CURRENT Hour #{{ round_number }}. This should focus on key events and observations from this hour, not previous hours. Your summary should be about 1-2 sentences long and should contain your ask.{% if seller_comms_enabled %} Include any messages you received from other sellers.{% endif %}>",
    {% if seller_comms_enabled and messaging_enabled %}
    "plan_for_message": "plan the message you are about to send",
    "message_to_sellers": "<message that will be received by other sellers at the start of the next hour before they place their asks (they will not react immediately to your message this hour) OR null if you don't wish to send a message.>",
    {% endif %}
    "scratch_pad_update": "<rewrite or extend your private Strategy Scratchpad here. Your output in this field should be an updated list in the following format '#### Observations, #### Thoughts, #### Updated Strategy'. This is your personal notebook. Output the full updated Strategy Scratchpad, and limit it to 250 words or less.>"
}

This is Hour #{{ round_number }} out of {{ num_rounds }} hours.
